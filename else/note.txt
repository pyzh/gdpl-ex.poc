#note103

test.exs 还是有用的

103: 
发现!?符号是直接用来结束函数名的，比如fff!a的a会当作参数

变量名不能大写

104: 可以使用一个空参数（比如[一]或[_]下划线），方便使用管道符/传递参数

总结出一个技巧，就是写代码的时候，同一个文件，创建一个副本。比如Ld2e和Ld2e2，先在Ld2e写，尽管写，不用管调试结果
写的差不多了，在把代码一段一段放到Ld2e2去测试运行。修改好的代码在替换回Ld2e。
（vscode里面，上下或左右两个窗口也是同理

 管道符，参数1换到参数2：  |> (& 印 &1).()

undefined function |/2? 是说 竖杠也可以定义么？不过试了一下，貌似要重载操作符，用宏