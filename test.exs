defmodule A do
    @a 1 ;  @b 2 ;  import Lib

    def run do t930();   t102a()  end

    def t930 do   @a + @b   end
    def t102a do
        印 'test' #Lib.印
        import DS;        全() 
        t102测Lib内函数()
    end
    def t102测Lib内函数 do
        v = [{:a, 1}]
        t = {:a, 1}
        # vL = 数据列表
        组员 v, :a
        组员 v, :a, 1
        循选 v,&()
    end
    def t102测Lib下所有函数的case do 
        :其余略 end 
    
end

IO.puts A.run